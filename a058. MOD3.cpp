/*
題目: a058. MOD3
難度: 初級
完成日期: 2026-01-29 22:47
功能: 輸入 n 個數字，統計除以 3 的餘數分別為 0, 1, 2 的個數。
*/

#include<bits/stdc++.h> // 引入標準函式庫
using namespace std;

int main(void){
	// n: 輸入數量, j: 迴圈變數
	// a: 儲存餘數為 0 的個數, b: 餘數為 1 的個數, c: 餘數為 2 的個數
	int n, j, a = 0, b = 0, c = 0;
	
	// 讀取接下來要處理的整數個數
	cin >> n;
	
	// 宣告一個長度為 n 的陣列來儲存輸入的數字
	int num[n];
	
	// 第一個迴圈：將所有數字讀入陣列中
	for(j = 0; j < n; j++){
		cin >> num[j];
	}
	
	// 第二個迴圈：遍歷陣列，判斷每個數字除以 3 的餘數
	for(j = 0; j < n; j++){
		// 如果整除（餘數為 0）
		if(num[j] % 3 == 0)
			a++;
		
		// 如果餘數為 1
		if(num[j] % 3 == 1)
			b++;
		
		// 如果餘數為 2
		if(num[j] % 3 == 2)
			c++;
	}
	
	// 依序輸出餘數為 0, 1, 2 的統計結果，中間以空格分隔
	cout << a << " " << b << " " << c;    
	
	return 0; // 建議加上回傳值，代表程式正常結束
}
